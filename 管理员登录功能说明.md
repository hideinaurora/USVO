# 管理员登录功能说明

## 功能概述

基于 MyBatis-Plus 生成的 AdminServiceImpl 实现了管理端用户登录功能，包含验证码验证。

## 已实现的文件

### 1. DTO 和 VO 类
- `LoginRequestDTO.java` - 登录请求参数
- `LoginResponseVO.java` - 登录响应数据
- `CaptchaVO.java` - 验证码响应数据

### 2. Service 层
- `AdminService.java` - 添加了 login() 和 generateCaptcha() 方法
- `AdminServiceImpl.java` - 实现了登录逻辑，包括：
  - 参数校验
  - 验证码校验（从 Redis 获取，验证后删除）
  - 用户查询和状态校验
  - MD5 密码加密验证
  - JWT Token 生成

### 3. Controller 层
- `AdminController.java` - 提供了以下接口：
  - `POST /api/admin/login` - 管理员登录
  - `GET /api/admin/captcha` - 获取验证码

### 4. 工具类
- `CaptchaUtil.java` - 生成图形验证码（Base64 格式）

### 5. 配置类
- `SwaggerConfig.java` - Swagger 3.0 (OpenAPI) 配置

### 6. 依赖更新
- 添加了 SpringDoc OpenAPI UI (Swagger 3.0)
- 添加了 Kaptcha 验证码依赖

## AdminEntity 结构

```java
- id: 用户ID（主键，自增）
- loginName: 登录名
- loginPassword: 登录密码（MD5 加密）
- userStatus: 用户状态（1-正常，0-禁用）
- isDeleted: 删除标识（逻辑删除）
- gmtCreate: 创建时间
- gmtModify: 修改时间
```

## 登录流程

1. **获取验证码**
   ```
   GET /api/admin/captcha
   ```
   响应：
   ```json
   {
     "code": 200,
     "message": "success",
     "data": {
       "captchaKey": "uuid字符串",
       "captchaImage": "data:image/png;base64,..."
     }
   }
   ```

2. **用户登录**
   ```
   POST /api/admin/login
   Content-Type: application/json
   ```
   请求体：
   ```json
   {
     "loginName": "admin",
     "loginPassword": "123456",
     "captchaKey": "步骤1返回的captchaKey",
     "captchaCode": "用户输入的验证码"
   }
   ```
   响应：
   ```json
   {
     "code": 200,
     "message": "success",
     "data": {
       "accessToken": "JWT令牌",
       "userId": 1,
       "loginName": "admin",
       "roleId": 1
     }
   }
   ```

## 验证码说明

- 验证码存储在 Redis 中，key 格式：`admin:captcha:{uuid}`
- 验证码有效期：5 分钟
- 验证成功后，验证码会被删除（一次性使用）
- 验证码格式：4 位字符（大小写字母+数字，去除易混淆字符）

## 安全特性

1. **验证码防护**：防止暴力破解
2. **密码加密**：使用 MD5 加密存储
3. **用户状态校验**：禁用用户无法登录
4. **JWT Token**：无状态认证，Token 有效期 720 分钟（12 小时）
5. **逻辑删除**：已删除用户无法登录

## 错误处理

系统会捕获以下异常并返回友好的错误信息：

- 参数不完整（登录名、密码、验证码为空）
- 验证码过期或错误
- 用户不存在
- 用户已被禁用
- 密码错误

错误响应格式：
```json
{
  "longResult": -1,
  "objResult": "错误信息"
}
```

## 使用说明

1. 确保 Redis 已启动（配置在 localhost:6379）
2. 确保 MySQL 数据库已连接（配置在 application-dev.yml）
3. 启动项目后，访问 Swagger 文档：**http://localhost:9013/swagger-ui.html**
4. 先调用 `/api/admin/captcha` 获取验证码
5. 再调用 `/api/admin/login` 进行登录

### Swagger 3.0 访问地址
- Swagger UI: http://localhost:9013/swagger-ui.html
- OpenAPI JSON: http://localhost:9013/v3/api-docs

## 数据库准备

确保 `sys_admin` 表中有测试数据：

```sql
INSERT INTO sys_admin (login_name, login_password, user_status, is_deleted, gmt_create, gmt_modify)
VALUES ('admin', MD5('123456'), 1, 0, NOW(), NOW());
```

测试账号：
- 登录名：admin
- 密码：123456

## Swagger 3.0 注解说明

- `@Tag` - 替代 `@Api`，用于标记控制器
- `@Operation` - 替代 `@ApiOperation`，用于标记接口方法
- SpringDoc OpenAPI 对 Spring Boot 2.x/3.x 有更好的兼容性
