# 参数校验优化说明

## 优化概述

移除了 DTO 注解校验和 Service 层逻辑校验的重复部分，统一使用 JSR-303 校验注解。

## 修改内容

### 1. DTO 层 - 添加 JSR-303 校验注解

#### ApplySaveDTO.java
```java
@NotBlank(message = "活动标题不能为空")
private String applyTitle;

@NotNull(message = "报名开始时间不能为空")
private LocalDateTime applyStartTime;

@NotNull(message = "报名结束时间不能为空")
private LocalDateTime applyEndTime;

@NotNull(message = "报名费用不能为空")
private Integer applyExpense;
```

#### ApplyUserQueryDTO.java
```java
@NotNull(message = "活动ID不能为空")
private Long applyId;
```

#### RefundExamineDTO.java
```java
@NotNull(message = "退款申请ID不能为空")
private Long refundId;

@NotNull(message = "审核类型不能为空")
private Integer examineType;
```

### 2. Controller 层 - 添加 @Valid 注解

#### ApplyController.java
```java
public ApiResponse<Long> add(@Valid @RequestBody ApplySaveDTO saveDTO)

public ApiResponse<Void> update(@Valid @RequestBody ApplySaveDTO saveDTO)

public ApiResponse<PageResult<ApplyUserDetailVO>> queryApplyUserPage(@Valid @RequestBody ApplyUserQueryDTO queryDTO)
```

#### RefundController.java
```java
public ApiResponse<PageResult<RefundDetailVO>> queryPage(@Valid @RequestBody RefundQueryDTO queryDTO)

public ApiResponse<Boolean> examine(@Valid @RequestBody RefundExamineDTO examineDTO)
```

### 3. Service 层 - 移除重复的参数校验

#### ApplyServiceImpl.java
**修改前（重复校验）：**
```java
public PageResult<ApplyUserDetailVO> queryApplyUserPage(ApplyUserQueryDTO queryDTO) {
    if (queryDTO.getApplyId() == null) {
        throw new CommonJsonException("活动ID不能为空");  // ❌ 重复校验
    }
    // ...
}
```

**修改后：**
```java
public PageResult<ApplyUserDetailVO> queryApplyUserPage(ApplyUserQueryDTO queryDTO) {
    // 参数已在 DTO 层通过 @NotNull 校验
    queryWrapper.eq("apply_id", queryDTO.getApplyId());  // ✅ 直接使用
    // ...
}
```

#### RefundServiceImpl.java
**修改前（重复校验）：**
```java
public Boolean examine(RefundExamineDTO examineDTO) {
    if (examineDTO.getRefundId() == null) {
        throw new CommonJsonException("退款申请ID不能为空");  // ❌ 重复校验
    }
    if (examineDTO.getExamineType() == null) {
        throw new CommonJsonException("审核类型不能为空");  // ❌ 重复校验
    }
    // ...
}
```

**修改后：**
```java
public Boolean examine(RefundExamineDTO examineDTO) {
    // 参数已在 DTO 层通过 @NotNull 校验，这里省略简单校验

    // 只保留业务规则校验
    if (examineDTO.getExamineType() != 1 && examineDTO.getExamineType() != 2) {
        throw new CommonJsonException("审核类型只能为1（审核通过）或2（审核拒绝）");
    }
    // ...
}
```

## 校验层次划分

### DTO 层（Controller 入口）
**职责：** 简单参数校验

**使用注解：**
- `@NotNull` - 非空校验
- `@NotBlank` - 字符串非空校验
- `@Min/@Max` - 数值范围校验
- `@Email` - 邮箱格式校验
- `@Pattern` - 正则表达式校验
- `@Size` - 长度校验

**触发时机：** Controller 接收请求时，Spring 自动校验

**示例：**
```java
@NotNull(message = "活动ID不能为空")
private Long applyId;

@NotBlank(message = "活动标题不能为空")
private String applyTitle;

@Min(value = 1, message = "报名费用必须大于0")
private Integer applyExpense;
```

### Service 层
**职责：** 业务规则校验

**校验内容：**
- 数据是否存在（如：退款申请是否存在）
- 业务状态是否满足操作条件（如：只能审核待审核的申请）
- 数据关联关系是否正确
- 业务规则是否满足

**示例：**
```java
// ✅ 业务规则校验
if (refundEntity.getExamineType() != 0) {
    throw new CommonJsonException("该退款申请已被审核，无法再次审核");
}

// ✅ 数据存在性校验
RefundEntity refundEntity = getById(refundId);
if (refundEntity == null) {
    throw new CommonJsonException("退款申请不存在");
}

// ✅ 业务规则校验
if (examineType != 1 && examineType != 2) {
    throw new CommonJsonException("审核类型只能为1（审核通过）或2（审核拒绝）");
}
```

## 校验流程

```
请求 → Controller (启用 @Valid) → Spring 自动校验 (DTO 注解) → Service (业务规则校验) → 数据库
                                    ↓ 校验失败                     ↓ 校验失败
                            返回 400 错误                抛出 CommonJsonException
```

## 校验时机对比

### DTO 层校验（Spring 自动）
**时机：** Controller 接收请求时

**示例：**
```java
// 请求：{"applyTitle": ""}

// @NotBlank 注解触发
// 返回：{"code": 400, "message": "活动标题不能为空"}
```

### Service 层校验（业务逻辑）
**时机：** 执行业务逻辑时

**示例：**
```java
// 业务规则：只能审核待审核的退款申请
if (refundEntity.getExamineType() != 0) {
    throw new CommonJsonException("该退款申请已被审核通过，无法再次审核");
}
```

## 常用校验注解

| 注解 | 说明 | 示例 |
|------|------|------|
| `@Null` | 必须为 null | `@Null(message = "ID必须为空")` |
| `@NotNull` | 必须非 null | `@NotNull(message = "ID不能为空")` |
| `@NotBlank` | 字符串非空（trim后） | `@NotBlank(message = "标题不能为空")` |
| `@NotEmpty` | 集合非空（字符串、集合、Map） | `@NotEmpty(message = "列表不能为空")` |
| `@Min` | 数值最小值 | `@Min(value = 1, message = "费用必须大于0")` |
| `@Max` | 数值最大值 | `@Max(value = 999999, message = "超出范围")` |
| `@Size` | 长度范围 | `@Size(min = 1, max = 100, message = "标题长度1-100字符")` |
| `@Email` | 邮箱格式 | `@Email(message = "邮箱格式不正确")` |
| `@Pattern` | 正则表达式 | `@Pattern(regexp = "^1[3-9]\\d{9}$", message = "手机号格式不正确")` |
| `@Past` | 日期必须是过去 | `@Past(message = "开始时间必须是过去时间")` |
| `@Future` | 日期必须是未来 | `@Future(message = "结束时间必须是未来时间")` |

## 特殊场景处理

### 场景1：新增 vs 修改

**问题：** ApplySaveDTO 的 applyId 在修改时才需要

**解决方案：** 保留 Service 层校验（因为这是业务逻辑）

```java
// DTO 层不加 @NotNull
private Long applyId;  // 新增时为空，修改时必填

// Service 层校验（业务规则）
if (saveDTO.getApplyId() == null) {
    // 新增逻辑
} else {
    // 修改逻辑，需要校验 applyId
}
```

### 场景2：条件必填

**问题：** 某些字段在其他字段满足条件时才必填

**解决方案：** 使用分组校验或 Service 层校验

```java
// 方案1：分组校验
@Null(groups = Create.class)
@NotNull(groups = Update.class)
private Long applyId;

// 方案2：Service 层校验（推荐）
// 根据业务逻辑在 Service 层校验
```

## 优化效果

### 修改前
```java
// DTO 层
public class RefundExamineDTO {
    // 无注解
    private Long refundId;
}

// Controller
public ApiResponse examine(@RequestBody RefundExamineDTO dto) { ... }

// Service 层
if (dto.getRefundId() == null) {  // ❌ 重复校验
    throw new CommonJsonException("退款申请ID不能为空");
}
```

### 修改后
```java
// DTO 层
@NotNull(message = "退款申请ID不能为空")  // ✅ 添加注解
private Long refundId;

// Controller
public ApiResponse examine(@Valid @RequestBody RefundExamineDTO dto) { ... }  // ✅ 启用校验

// Service 层
// 参数已校验，直接使用，只保留业务规则校验
```

## 测试验证

### 测试1：DTO 参数为空
```bash
curl -X POST http://localhost:9013/api/apply/users/page \
  -H "Content-Type: application/json" \
  -H "token: ..." \
  -d '{}'
```

**响应：**
```json
{
  "code": 400,
  "message": "活动ID不能为空",
  "data": null
}
```

### 测试2：DTO 参数校验通过，业务规则不满足
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: ..." \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "code": -1,
  "message": "该退款申请已被审核通过，无法再次审核",
  "data": null
}
```

## 优势

1. ✅ **分层清晰**：DTO 层负责格式校验，Service 层负责业务规则校验
2. ✅ **减少重复**：避免在 Service 层重复校验 DTO 已校验的内容
3. ✅ **自动校验**：Spring 自动处理校验，无需手动代码
4. **统一规范**：使用 JSR-303 标准注解
5. **更好的错误提示**：校验失败时，Spring 会返回具体的字段错误信息

## 注意事项

1. **@Valid 注解**：必须在 Controller 的 @RequestBody 参数前添加 @Valid，否则校验不会生效
2. **校验顺序**：DTO 校验 → Service 校验，前者失败直接返回 400，后者返回 -1
3. **业务校验**：复杂业务规则仍在 Service 层校验，不应简单移到 DTO 层
4. **日志记录**：Service 层的业务规则校验可以添加日志，便于追踪
