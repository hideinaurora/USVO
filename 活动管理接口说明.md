# 活动管理接口说明

## 功能概述

基于 ApplyEntity 实现活动的 CRUD 接口，查询接口自动关联统计：
- 报名成功人数（is_pay = 1）
- 支付金额总数（pay_status = 1）
- 退款成功人数（examine_type = 1）
- 退款金额总数（examine_type = 1）

## 认证说明

所有接口都需要 JWT Token 认证，在请求头中携带：
```
token: eyJhbGciOiJIUzI1NiJ9...
```

详细的认证流程请查看 [JWT权限校验说明.md](./JWT权限校验说明.md)

## 实体结构

### ApplyEntity (activity_apply) - 活动表
```java
- applyId: 活动ID（主键）
- applyTitle: 活动标题
- applyStartTime: 报名开始时间
- applyEndTime: 报名结束时间
- applyExpense: 报名费用（分）
- activeInfo: 活动详情
- limitNum: 限制人数
- gmtCreate: 创建时间
- gmtModify: 修改时间
- isDeleted: 逻辑删除
```

### ApplyUserEntity (activity_apply_user) - 报名记录表
```java
- id: 记录ID
- userId: 用户ID
- applyId: 活动ID
- isPay: 支付状态（0-未支付，1-已支付）
```

### ApplyPayEntity (activity_apply_pay) - 支付订单表
```java
- id: 订单ID
- userId: 用户ID
- applyId: 活动ID
- payStatus: 支付状态
- totalAmount: 总金额（分）
```

### RefundEntity (activity_refund) - 退款申请表
```java
- id: 退款ID
- userId: 用户ID
- applyId: 活动ID
- examineType: 审核类型（0-待审核，1-审核通过，2-审核拒绝）
```

## API 接口

### 权限说明

| 接口 | 方法 | 权限要求 | 说明 |
|------|------|---------|------|
| /api/apply/page | POST | ADMIN | 分页查询（仅管理员） |
| /api/apply/list | GET | ADMIN | 列表查询（仅管理员） |
| /api/apply/{applyId} | GET | ADMIN, USER | 详情查询（管理员和用户） |
| /api/apply/add | POST | ADMIN | 新增活动（仅管理员） |
| /api/apply/update | PUT | ADMIN | 修改活动（仅管理员） |
| /api/apply/{applyIds} | DELETE | ADMIN | 删除活动（仅管理员） |

### 1. 分页查询活动列表
**接口：** `POST /api/apply/page`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "pageNum": 1,
  "pageSize": 10,
  "applyTitle": "篮球",
  "startTimeBegin": "2026-03-01 00:00:00",
  "startTimeEnd": "2026-03-31 23:59:59"
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 50,
    "totalPages": 5,
    "records": [
      {
        "applyId": 1,
        "applyTitle": "春季篮球训练营",
        "applyStartTime": "2026-03-15 09:00:00",
        "applyEndTime": "2026-03-20 18:00:00",
        "applyExpense": 9900,
        "activeInfo": "本次活动旨在...",
        "limitNum": 100,
        "gmtCreate": "2026-03-13 10:00:00",
        "gmtModify": "2026-03-13 10:00:00",
        "paidUserCount": 50,
        "totalPayAmount": 495000,
        "totalPayAmountYuan": 4950.0,
        "refundSuccessCount": 2,
        "refundTotalAmount": 19800,
        "refundTotalAmountYuan": 198.0
      }
    ]
  }
}
```

### 2. 查询活动列表（不分页）
**接口：** `GET /api/apply/list`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "applyId": 1,
      "applyTitle": "春季篮球训练营",
      "paidUserCount": 50,
      "totalPayAmount": 495000,
      "totalPayAmountYuan": 4950.0,
      "refundSuccessCount": 2,
      ...
    }
  ]
}
```

### 3. 查询活动详情
**接口：** `GET /api/apply/{applyId}`

**权限：** 管理员和用户（ADMIN, USER）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**路径参数：**
- `applyId`: 活动ID

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "applyId": 1,
    "applyTitle": "春季篮球训练营",
    "paidUserCount": 50,
    "totalPayAmount": 495000,
    "totalPayAmountYuan": 4950.0,
    "refundSuccessCount": 2,
    ...
  }
}
```

### 4. 新增活动
**接口：** `POST /api/apply/add`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "applyTitle": "春季篮球训练营",
  "applyStartTime": "2026-03-15 09:00:00",
  "applyEndTime": "2026-03-20 18:00:00",
  "applyExpense": 9900,
  "activeInfo": "本次活动旨在提高青少年篮球技能...",
  "limitNum": 100
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "新增成功",
  "data": 1
}
```

### 5. 修改活动
**接口：** `PUT /api/apply/update`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "applyId": 1,
  "applyTitle": "夏季篮球训练营",
  "applyStartTime": "2026-06-15 09:00:00",
  "applyEndTime": "2026-06-20 18:00:00",
  "applyExpense": 12900,
  "activeInfo": "更新后的活动详情",
  "limitNum": 150
}
```

**响应示例：**
```json
{
  "code": 200,
  "message": "修改成功",
  "data": null
}
```

### 6. 删除活动
**接口：** `DELETE /api/apply/{applyIds}`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**路径参数：**
- `applyIds`: 活动ID（多个用逗号分隔），例如：`1,2,3`

**业务规则：**
- 如果活动已存在报名记录，则不允许删除
- 删除操作为逻辑删除，数据不会真正从数据库删除

**响应示例：**
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

**错误示例：**
```json
{
  "longResult": -1,
  "objResult": "该活动已存在报名记录，不允许删除"
}
```

## 统计字段说明

查询接口会自动统计以下信息：

| 字段 | 说明 | 数据来源 | 统计条件 |
|------|------|---------|---------|
| `paidUserCount` | 报名成功人数 | activity_apply_user | is_pay = 1 |
| `totalPayAmount` | 支付金额总数（分） | activity_apply_pay | pay_status = 1 |
| `totalPayAmountYuan` | 支付金额总数（元） | 自动计算 | totalPayAmount / 100 |
| `refundSuccessCount` | 退款成功人数 | activity_refund | examine_type = 1 |
| `refundTotalAmount` | 退款金额总数（分） | activity_refund | examine_type = 1 |
| `refundTotalAmountYuan` | 退款金额总数（元） | 自动计算 | refundTotalAmount / 100 |

## 查询条件

### 分页查询支持的条件：
- `applyTitle`: 活动标题（模糊查询）
- `startTimeBegin`: 报名开始时间（起始）
- `startTimeEnd`: 报名开始时间（结束）

### 排序规则：
- 默认按创建时间倒序（最新的在前）

## 数据示例

### 查询场景示例：

1. **查询标题包含"篮球"的活动**
```json
{
  "pageNum": 1,
  "pageSize": 10,
  "applyTitle": "篮球"
}
```

2. **查询3月份开始的活动**
```json
{
  "pageNum": 1,
  "pageSize": 10,
  "startTimeBegin": "2026-03-01 00:00:00",
  "startTimeEnd": "2026-03-31 23:59:59"
}
```

3. **组合查询**
```json
{
  "pageNum": 1,
  "pageSize": 10,
  "applyTitle": "篮球",
  "startTimeBegin": "2026-03-01 00:00:00",
  "startTimeEnd": "2026-03-31 23:59:59"
}
```

## 使用 Swagger 测试

访问 Swagger 文档：http://localhost:9013/swagger-ui.html

在"活动管理"标签下可以测试所有接口。

## 注意事项

1. **费用单位**：所有金额字段单位为"分"，前端显示时需要除以100转换为"元"
2. **时间格式**：使用 `yyyy-MM-dd HH:mm:ss` 格式
3. **批量删除**：删除接口支持批量，多个ID用英文逗号分隔
4. **逻辑删除**：删除操作为逻辑删除，数据不会真正从数据库删除
5. **统计实时性**：统计信息每次查询时实时计算，确保数据准确性
6. **删除限制**：如果活动已存在报名记录，则不允许删除
