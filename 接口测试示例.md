# 活动管理接口测试示例

## 测试环境

- 基础URL: `http://localhost:9013`
- 管理员账号: `admin` / `123456`

## 测试步骤

### 1. 获取验证码

```bash
curl -X GET http://localhost:9013/api/admin/captcha
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "captchaKey": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "captchaImage": "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAA..."
  }
}
```

### 2. 管理员登录

保存上面返回的 `captchaKey`，并识别验证码图片中的字符。

```bash
curl -X POST http://localhost:9013/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginName": "admin",
    "loginPassword": "123456",
    "captchaKey": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "captchaCode": "A3b9"
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiJ9.eyJzZWVkIjoie1wiYWNjb3VudElkXCI6MSxcInJvbGVJZFwiOjF9IiwiaWF0IjoxNzQxOTk1MjAwLCJleHAiOjE3NDIwMDI0MDB9.xxx...",
    "userId": 1,
    "loginName": "admin",
    "roleId": 1
  }
}
```

**保存 `accessToken`，后续所有接口都需要使用！**

### 3. 分页查询活动列表

```bash
curl -X POST http://localhost:9013/api/apply/page \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "pageNum": 1,
    "pageSize": 10
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 1,
    "totalPages": 1,
    "records": [
      {
        "applyId": 1,
        "applyTitle": "春季篮球训练营",
        "applyStartTime": "2026-03-15 09:00:00",
        "applyEndTime": "2026-03-20 18:00:00",
        "applyExpense": 9900,
        "activeInfo": "本次活动旨在提高青少年篮球技能",
        "limitNum": 100,
        "gmtCreate": "2026-03-13 10:00:00",
        "gmtModify": "2026-03-13 10:00:00",
        "paidUserCount": 50,
        "totalPayAmount": 495000,
        "totalPayAmountYuan": 4950.0,
        "refundSuccessCount": 2,
        "refundTotalAmount": 19800,
        "refundTotalAmountYuan": 198.0
      }
    ]
  }
}
```

### 4. 查询活动列表（不分页）

```bash
curl -X GET http://localhost:9013/api/apply/list \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

### 5. 查询活动详情

```bash
curl -X GET http://localhost:9013/api/apply/1 \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

### 6. 新增活动

```bash
curl -X POST http://localhost:9013/api/apply/add \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "applyTitle": "夏季足球训练营",
    "applyStartTime": "2026-06-15 09:00:00",
    "applyEndTime": "2026-06-20 18:00:00",
    "applyExpense": 8800,
    "activeInfo": "足球基础训练，适合6-12岁儿童",
    "limitNum": 50
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "新增成功",
  "data": 2
}
```

### 7. 修改活动

```bash
curl -X PUT http://localhost:9013/api/apply/update \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "applyId": 2,
    "applyTitle": "夏季足球训练营（进阶班）",
    "applyStartTime": "2026-06-15 09:00:00",
    "applyEndTime": "2026-06-20 18:00:00",
    "applyExpense": 9800,
    "activeInfo": "足球进阶训练，适合有一定基础的学员",
    "limitNum": 40
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 8. 删除活动（单个）

```bash
curl -X DELETE http://localhost:9013/api/apply/2 \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 9. 删除活动（批量）

```bash
curl -X DELETE "http://localhost:9013/api/apply/3,4,5" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": null
}
```

### 10. 删除失败（有报名记录）

```bash
curl -X DELETE http://localhost:9013/api/apply/1 \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "该活动已存在报名记录，不允许删除"
}
```

## Postman 测试

### 1. 创建环境变量

在 Postman 中创建环境变量：
- `baseUrl`: `http://localhost:9013`
- `token`: (登录后自动填充)

### 2. 创建请求集合

#### 请求1：获取验证码
- Method: `GET`
- URL: `{{baseUrl}}/api/admin/captcha`
- Tests: 自动保存 captchaKey

```javascript
var jsonData = pm.response.json();
pm.environment.set("captchaKey", jsonData.data.captchaKey);
```

#### 请求2：登录
- Method: `POST`
- URL: `{{baseUrl}}/api/admin/login`
- Headers:
  ```
  Content-Type: application/json
  ```
- Body (raw JSON):
  ```json
  {
    "loginName": "admin",
    "loginPassword": "123456",
    "captchaKey": "{{captchaKey}}",
    "captchaCode": "输入验证码"
  }
  ```
- Tests: 自动保存 token

```javascript
var jsonData = pm.response.json();
if (jsonData.code === 200) {
    pm.environment.set("token", jsonData.data.accessToken);
}
```

#### 请求3：分页查询
- Method: `POST`
- URL: `{{baseUrl}}/api/apply/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "pageNum": 1,
    "pageSize": 10
  }
  ```

#### 请求4：新增活动
- Method: `POST`
- URL: `{{baseUrl}}/api/apply/add`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "applyTitle": "测试活动",
    "applyStartTime": "2026-03-15 09:00:00",
    "applyEndTime": "2026-03-20 18:00:00",
    "applyExpense": 9900,
    "activeInfo": "测试活动详情",
    "limitNum": 100
  }
  ```

## 错误测试

### 1. Token 缺失

```bash
curl -X POST http://localhost:9013/api/apply/page \
  -H "Content-Type: application/json" \
  -d '{
    "pageNum": 1,
    "pageSize": 10
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

### 2. Token 无效

```bash
curl -X POST http://localhost:9013/api/apply/page \
  -H "Content-Type: application/json" \
  -H "token: invalid_token_123" \
  -d '{
    "pageNum": 1,
    "pageSize": 10
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

### 3. 权限不足（普通用户访问管理员接口）

使用 roleId=2 的用户 Token 访问只有管理员才能访问的接口。

**响应：**
```json
{
  "longResult": -1,
  "objResult": "权限不符"
}
```

## 注意事项

1. **Token 有效期**：720分钟（12小时），过期需要重新登录
2. **验证码**：验证码5分钟内有效，使用一次后失效
3. **删除限制**：活动已有报名记录则不允许删除
4. **时间格式**：`yyyy-MM-dd HH:mm:ss`
5. **金额单位**：所有金额单位为"分"
6. **分页参数**：pageNum 和 pageSize 默认值为 1 和 10
