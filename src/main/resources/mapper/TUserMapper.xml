<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.TUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.example.entity.TUserEntity">
        <id column="id" property="id" />
        <result column="user_name" property="userName" />
        <result column="tele_phone" property="telePhone" />
        <result column="temp_state" property="tempState" />
        <result column="reg_status" property="regStatus" />
        <result column="open_id" property="openId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_name, tele_phone, temp_state, reg_status, open_id
    </sql>

</mapper>
