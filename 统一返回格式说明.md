# 统一返回格式说明

## 返回格式统一

现在所有接口（包括正常响应和异常响应）都使用统一的 `ApiResponse` 格式。

## 统一格式

### 成功响应

```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

### 失败响应（业务异常）

```json
{
  "code": -1,
  "message": "错误信息",
  "data": null
}
```

### 失败响应（系统异常）

```json
{
  "code": 500,
  "message": "服务器异常，请稍后重试",
  "data": null
}
```

## HTTP 状态码说明

| code | 说明 | 场景 |
|------|------|------|
| 200 | 成功 | 业务正常执行 |
| 400 | 参数错误 | 参数校验失败 |
| 401 | 未登录 | Token 缺失或无效 |
| 403 | 权限不足 | 用户权限不足 |
| 405 | 请求方式错误 | GET/POST 方法错误 |
| 500 | 服务器错误 | 系统内部异常 |
| -1 | 业务异常 | CommonJsonException 抛出的业务异常 |

## 各种异常的返回格式

### 1. CommonJsonException（业务异常）

**抛出方式：**
```java
throw new CommonJsonException("活动ID不能为空");
```

**返回格式：**
```json
{
  "code": -1,
  "message": "活动ID不能为空",
  "data": null
}
```

**使用场景：**
- 业务参数校验失败
- 业务规则不满足
- 数据不存在或冲突

### 2. 参数校验异常

**返回格式：**
```json
{
  "code": 400,
  "message": "字段校验失败",
  "data": {
    "applyId": "活动ID不能为空",
    "applyTitle": "活动标题不能为空"
  }
}
```

### 3. 未登录异常

**抛出方式：**
```java
throw new UnauthenticatedException();
```

**返回格式：**
```json
{
  "code": 401,
  "message": "登录失效请重新登录",
  "data": null
}
```

### 4. 权限不足异常

**抛出方式：**
```java
throw new UnauthorizedException();
```

**返回格式：**
```json
{
  "code": 403,
  "message": "权限不符",
  "data": null
}
```

### 5. 请求方式错误

**返回格式：**
```json
{
  "code": 405,
  "message": "请求方式不符",
  "data": null
}
```

### 6. 系统异常

**返回格式：**
```json
{
  "code": 500,
  "message": "服务器异常，请稍后重试",
  "data": null
}
```

## 前端使用建议

### 判断接口是否成功

```javascript
// 示例1：使用 code 判断
if (response.code === 200) {
  // 成功
  console.log(response.message);
  const data = response.data;
} else {
  // 失败
  console.error(response.message);
}

// 示例2：使用 HTTP 状态码判断
axios.post('/api/apply/add', data)
  .then(response => {
    if (response.data.code === 200) {
      // 成功
    } else {
      // 业务异常
      alert(response.data.message);
    }
  })
  .catch(error => {
    // 系统异常
    console.error(error);
  });
```

### 统一错误处理

```javascript
// Axios 拦截器
axios.interceptors.response.use(
  response => {
    const { code, message, data } = response.data;
    if (code === 200) {
      return data;
    } else {
      // 业务异常
      Message.error(message);
      return Promise.reject(new Error(message));
    }
  },
  error => {
    // 系统异常或网络错误
    if (error.response) {
      const { status, data } = error.response;
      Message.error(data.message || '请求失败');
    } else {
      Message.error('网络错误');
    }
    return Promise.reject(error);
  }
);
```

## 后端使用建议

### 返回成功响应

```java
// 无数据返回
return ApiResponse.success();

// 带数据返回
return ApiResponse.success(data);

// 自定义消息
return ApiResponse.success("新增成功", applyId);
```

### 抛出业务异常

```java
// 简单异常
throw new CommonJsonException("活动ID不能为空");

// 带详细错误信息
throw new CommonJsonException("该活动已存在报名记录，不允许删除");
```

### 返回错误响应（不抛异常）

```java
// 使用默认500错误码
return ApiResponse.error("操作失败");

// 自定义错误码
return ApiResponse.error(400, "参数错误");
```

## 响应格式对比

### 修改前（旧格式）

**成功：**
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

**失败：**
```json
{
  "longResult": -1,
  "objResult": "错误信息"
}
```

### 修改后（统一格式）

**成功：**
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

**失败：**
```json
{
  "code": -1,
  "message": "错误信息",
  "data": null
}
```

## 迁移说明

### 无需修改的代码

1. **Controller 返回值** - 已经使用 `ApiResponse<T>`，无需修改
2. **前端判断逻辑** - 如果使用 `response.code === 200` 判断成功，无需修改

### 需要修改的代码

如果前端代码使用了旧的响应格式：

```javascript
// 旧代码（需要修改）
if (response.longResult === -1) {
  alert(response.objResult);
}

// 新代码（统一格式）
if (response.code !== 200) {
  alert(response.message);
}
```

## 测试验证

### 测试成功响应

```bash
curl -X POST http://localhost:9013/api/apply/add \
  -H "Content-Type: application/json" \
  -H "token: ..." \
  -d '{
    "applyTitle": "测试活动",
    "applyStartTime": "2026-03-15 09:00:00",
    "applyEndTime": "2026-03-20 18:00:00",
    "applyExpense": 9900
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": 1
}
```

### 测试业务异常

```bash
# 参数为空
curl -X POST http://localhost:9013/api/apply/add \
  -H "Content-Type: application/json" \
  -H "token: ..." \
  -d '{}'
```

**响应：**
```json
{
  "code": -1,
  "message": "活动标题不能为空",
  "data": null
}
```

### 测试认证异常

```bash
# Token 缺失
curl -X POST http://localhost:9013/api/apply/page \
  -H "Content-Type: application/json" \
  -d '{
    "pageNum": 1,
    "pageSize": 10
  }'
```

**响应：**
```json
{
  "code": 401,
  "message": "登录失效请重新登录",
  "data": null
}
```

## 注意事项

1. **统一性**：所有接口（成功和失败）都使用 `ApiResponse` 格式
2. **错误码**：
   - 业务异常统一使用 `code = -1`
   - HTTP 标准状态码：400、401、403、405、500
3. **错误信息**：`message` 字段包含详细的错误描述
4. **兼容性**：前端代码如果依赖旧的 `longResult/objResult` 格式需要修改

## 优势

1. ✅ **格式统一**：成功和失败响应结构一致
2. ✅ **易于解析**：前端只需处理一种数据格式
3. ✅ **RESTful**：符合 RESTful API 设计规范
4. ✅ **可扩展**：支持自定义错误码和错误消息
5. ✅ **类型安全**：泛型支持，编译时类型检查
