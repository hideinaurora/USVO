# 退款管理接口说明

## 功能概述

提供查询退款学生列表的接口，支持分页查询和不分页查询，自动关联用户表获取姓名、活动表获取活动标题、审核记录表获取审核信息。

## 数据来源

- 主表：`activity_refund` (退款申请表)
- 关联表：
  - `basic_user` (用户信息表)
  - `activity_apply` (活动表)
  - `activity_refund_examine` (退款审核记录表)

## API 接口

### 1. 分页查询退款学生列表

**接口：** `POST /api/refund/page`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10,
  "examineType": 0,
  "userName": "张三"
}
```

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| applyId | Long | 否 | 活动ID |
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认10 |
| examineType | Integer | 否 | 审核状态：0-待审核，1-审核通过，2-审核拒绝 |
| userName | String | 否 | 用户名（模糊查询） |

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 20,
    "totalPages": 2,
    "records": [
      {
        "id": 1,
        "userId": 100,
        "userName": "张三",
        "applyId": 1,
        "applyTitle": "春季篮球训练营",
        "merOrderId": "202603131003001",
        "refundNo": "REFUND202603131001",
        "outRefundNo": "OUT202603131001",
        "refundAmount": 9900,
        "refundAmountYuan": 99.0,
        "totalAmount": 9900,
        "totalAmountYuan": 99.0,
        "refundTime": "2026-03-13 10:30:00",
        "examineType": 0,
        "examineTypeDesc": "待审核",
        "bz": "用户申请退款",
        "failMessage": null,
        "gmtCreate": "2026-03-13 10:00:00",
        "gmtModify": "2026-03-13 10:00:00",
        "examineId": 1,
        "examineReason": null,
        "adminId": null,
        "examineTime": null
      }
    ]
  }
}
```

### 3. 审核退款申请

**接口：** `POST /api/refund/examine`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "refundId": 1,
  "examineType": 1,
  "reason": "用户提供了相关证明材料，同意退款申请"
}
```

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| refundId | Long | 是 | 退款申请ID |
| examineType | Integer | 是 | 审核类型：1-审核通过，2-审核拒绝 |
| reason | String | 否 | 审核理由/说明 |

**响应示例：**
```json
{
  "code": 200,
  "message": "审核成功",
  "data": null
}
```

**错误示例：**

1. 退款申请不存在
```json
{
  "longResult": -1,
  "objResult": "退款申请不存在"
}
```

2. 退款申请已被审核
```json
{
  "longResult": -1,
  "objResult": "该退款申请已被审核通过，无法再次审核"
}
```

3. 审核类型错误
```json
{
  "longResult": -1,
  "objResult": "审核类型只能为1（审核通过）或2（审核拒绝）"
}
```

### 3. 审核退款申请

**接口：** `POST /api/refund/examine`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "refundId": 1,
  "examineType": 1,
  "reason": "用户提供了相关证明材料，同意退款申请"
}
```

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| refundId | Long | 是 | 退款申请ID |
| examineType | Integer | 是 | 审核类型：1-审核通过，2-审核拒绝 |
| reason | String | 否 | 审核理由/说明 |

**响应示例：**
```json
{
  "code": 200,
  "message": "审核成功",
  "data": null
}
```

**错误示例：**

1. 退款申请不存在
```json
{
  "longResult": -1,
  "objResult": "退款申请不存在"
}
```

2. 退款申请已被审核
```json
{
  "longResult": -1,
  "objResult": "该退款申请已被审核通过，无法再次审核"
}
```

3. 审核类型错误
```json
{
  "longResult": -1,
  "objResult": "审核类型只能为1（审核通过）或2（审核拒绝）"
}
```

### 4. 查询退款学生列表（不分页）

**接口：** `GET /api/refund/list`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 100,
      "userName": "张三",
      "applyId": 1,
      "applyTitle": "春季篮球训练营",
      "examineType": 1,
      "examineTypeDesc": "审核通过",
      "refundAmount": 9900,
      "refundAmountYuan": 99.0,
      "examineId": 1,
      "examineReason": "同意退款",
      "adminId": 1,
      "examineTime": "2026-03-13 11:00:00"
    }
  ]
}
```

## 返回字段说明

### 基础信息

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 退款申请ID |
| userId | Long | 用户ID |
| userName | String | 用户名（关联用户表） |
| applyId | Long | 活动ID |
| applyTitle | String | 活动标题（关联活动表） |

### 退款信息

| 字段 | 类型 | 说明 |
|------|------|------|
| merOrderId | String | 商户订单号 |
| refundNo | String | 退款单号 |
| outRefundNo | String | 外部退款单号 |
| refundAmount | Integer | 退款金额（分） |
| refundAmountYuan | Double | 退款金额（元） |
| totalAmount | Integer | 实付金额（分） |
| totalAmountYuan | Double | 实付金额（元） |
| refundTime | LocalDateTime | 退款时间 |

### 审核信息

| 字段 | 类型 | 说明 |
|------|------|------|
| examineType | Integer | 审核状态：0-待审核，1-审核通过，2-审核拒绝 |
| examineTypeDesc | String | 审核状态描述 |
| bz | String | 备注 |
| failMessage | String | 失败消息 |

### 审核记录信息

| 字段 | 类型 | 说明 |
|------|------|------|
| examineId | Long | 审核记录ID |
| examineReason | String | 审核理由/说明 |
| adminId | Long | 审核管理员ID |
| examineTime | LocalDateTime | 审核时间 |

### 系统信息

| 字段 | 类型 | 说明 |
|------|------|------|
| gmtCreate | LocalDateTime | 创建时间 |
| gmtModify | LocalDateTime | 修改时间 |

## 查询场景

### 1. 查询所有退款记录（分页）

```json
{
  "pageNum": 1,
  "pageSize": 10
}
```

### 2. 查询指定活动的退款记录

```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10
}
```

### 3. 查询待审核的退款记录

```json
{
  "examineType": 0,
  "pageNum": 1,
  "pageSize": 10
}
```

### 4. 查询审核通过的退款记录

```json
{
  "examineType": 1,
  "pageNum": 1,
  "pageSize": 10
}
```

### 5. 按用户名模糊查询

```json
{
  "userName": "张",
  "pageNum": 1,
  "pageSize": 10
}
```

### 6. 组合查询

```json
{
  "applyId": 1,
  "examineType": 0,
  "userName": "张三",
  "pageNum": 1,
  "pageSize": 10
}
```

## 实现逻辑

1. **查询退款记录**：从 `activity_refund` 表查询退款申请
2. **批量查询用户信息**：从 `basic_user` 表根据 userId 批量查询用户信息
3. **批量查询活动信息**：从 `activity_apply` 表根据 applyId 批量查询活动信息
4. **批量查询审核记录**：从 `activity_refund_examine` 表根据 refundId 批量查询审核记录（取最新一条）
5. **数据组装**：将所有关联信息组装成 VO
6. **分页返回**：支持分页和不分页两种方式

## 审核状态说明

| 状态码 | 状态描述 | 说明 |
|--------|---------|------|
| 0 | 待审核 | 用户已提交退款申请，等待管理员审核 |
| 1 | 审核通过 | 管理员审核通过，退款成功 |
| 2 | 审核拒绝 | 管理员审核拒绝，退款失败 |

## 性能优化

- ✅ 使用批量查询避免 N+1 问题
- ✅ 一次性查询所有关联数据
- ✅ 通过 Map 进行数据匹配
- ✅ 支持分页查询，避免一次性加载大量数据
- ✅ 审核记录取最新一条，避免重复数据

## 使用示例

### curl 示例

```bash
# 分页查询所有退款记录
curl -X POST http://localhost:9013/api/refund/page \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "pageNum": 1,
    "pageSize": 10
  }'

# 查询待审核的退款记录
curl -X POST http://localhost:9013/api/refund/page \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "applyId": 1,
    "examineType": 0,
    "pageNum": 1,
    "pageSize": 10
  }'

# 查询所有（不分页）
curl -X GET http://localhost:9013/api/refund/list \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."

# 审核退款申请（通过）
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "用户提供了相关证明材料，同意退款申请"
  }'

# 审核退款申请（拒绝）
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 2,
    "examineType": 2,
    "reason": "退款原因不充分，拒绝申请"
  }'
```

### Postman 配置

**分页查询：**
- Method: `POST`
- URL: `http://localhost:9013/api/refund/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "applyId": 1,
    "examineType": 0,
    "pageNum": 1,
    "pageSize": 10
  }
  ```

**审核退款申请：**
- Method: `POST`
- URL: `http://localhost:9013/api/refund/examine`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "refundId": 1,
    "examineType": 1,
    "reason": "用户提供了相关证明材料，同意退款申请"
  }
  ```

**查询所有：**
- Method: `GET`
- URL: `http://localhost:9013/api/refund/list`
- Headers:
  ```
  token: {{token}}
  ```

## 接口汇总

| 接口 | 方法 | 权限要求 | 说明 |
|------|------|---------|------|
| /api/refund/page | POST | ADMIN | 分页查询退款记录 |
| /api/refund/list | GET | ADMIN | 查询所有退款记录 |
| /api/refund/examine | POST | ADMIN | 审核退款申请 |
- Method: `POST`
- URL: `http://localhost:9013/api/refund/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "applyId": 1,
    "examineType": 0,
    "pageNum": 1,
    "pageSize": 10
  }
  ```

**查询所有：**
- Method: `GET`
- URL: `http://localhost:9013/api/refund/list`
- Headers:
  ```
  token: {{token}}
  ```

## 错误处理

### 1. Token 缺失或无效

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

### 2. 权限不足

**响应：**
```json
{
  "code": 403,
  "message": "权限不符",
  "data": null
}
```

### 3. 查询无结果

如果查询条件匹配不到任何数据，返回空列表：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 0,
    "totalPages": 0,
    "records": []
  }
}
```

## 数据统计

通过本接口可以方便地统计：
- 退款申请总数
- 待审核退款数量（examineType = 0）
- 审核通过退款数量（examineType = 1）
- 审核拒绝退款数量（examineType = 2）
- 各活动的退款情况
- 退款金额统计

## 业务流程

### 退款申请流程

1. **用户提交退款申请**
   - 创建 `activity_refund` 记录（examineType = 0）

2. **管理员审核退款**
   - 查询待审核的退款申请（examineType = 0）
   - 调用审核接口 `POST /api/refund/examine`
   - 传入参数：refundId、examineType（1-通过，2-拒绝）、reason

3. **系统更新审核状态**
   - 更新 `activity_refund` 的 examineType（1-通过，2-拒绝）
   - 创建 `activity_refund_examine` 审核记录
   - 记录审核管理员ID和审核时间

### 审核业务规则

1. **只能审核待审核的申请**
   - examineType = 0 的退款申请可以审核
   - examineType = 1 或 2 的申请无法再次审核

2. **审核类型**
   - examineType = 1：审核通过，退款成功
   - examineType = 2：审核拒绝，退款失败

3. **审核记录**
   - 每次审核都会创建一条审核记录
   - 记录审核管理员ID、审核结果、审核理由
   - 可以查询历史审核记录

### 查询关联说明

- **userId** → 关联 `basic_user.id` 获取 `user_name`
- **applyId** → 关联 `activity_apply.apply_id` 获取 `apply_title`
- **id (refundId)** → 关联 `activity_refund_examine.refund_id` 获取审核记录

## 扩展功能

可以根据业务需求扩展：
- 添加退款审核接口
- 添加退款统计接口
- 支持导出 Excel 功能
- 添加退款时间范围筛选
- 添加退款金额范围筛选
