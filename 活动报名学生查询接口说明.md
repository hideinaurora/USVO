# 活动报名学生查询接口说明

## 功能概述

提供查询活动报名学生列表的接口，支持分页查询和不分页查询两种方式，自动关联用户表获取报名人员姓名。

## 数据来源

- 主表：`activity_apply_user` (报名记录表)
- 关联表：`basic_user` (用户信息表)

## API 接口

### 1. 分页查询活动报名学生列表

**接口：** `POST /api/apply/users/page`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10,
  "isPay": 1,
  "userName": "张三"
}
```

**参数说明：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| applyId | Long | 是 | 活动ID |
| pageNum | Integer | 否 | 页码，默认1 |
| pageSize | Integer | 否 | 每页数量，默认10 |
| isPay | Integer | 否 | 支付状态：0-未支付，1-已支付 |
| userName | String | 否 | 用户名（模糊查询） |

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "pageNum": 1,
    "pageSize": 10,
    "total": 50,
    "totalPages": 5,
    "records": [
      {
        "id": 1,
        "userId": 100,
        "userName": "张三",
        "loginName": "zhangsan",
        "applyId": 1,
        "isPay": 1,
        "isPayDesc": "已支付",
        "userStatus": 1,
        "userStatusDesc": "正常",
        "gmtCreate": "2026-03-13 10:30:00",
        "gmtModify": "2026-03-13 10:30:00"
      }
    ]
  }
}
```

### 2. 查询活动报名学生列表（不分页）

**接口：** `GET /api/apply/users/{applyId}`

**权限：** 仅管理员（ADMIN）

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**路径参数：**
- `applyId`: 活动ID

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 100,
      "userName": "张三",
      "loginName": "zhangsan",
      "applyId": 1,
      "isPay": 1,
      "isPayDesc": "已支付",
      "userStatus": 1,
      "userStatusDesc": "正常",
      "gmtCreate": "2026-03-13 10:30:00",
      "gmtModify": "2026-03-13 10:30:00"
    },
    {
      "id": 2,
      "userId": 101,
      "userName": "李四",
      "loginName": "lisi",
      "applyId": 1,
      "isPay": 0,
      "isPayDesc": "未支付",
      "userStatus": 1,
      "userStatusDesc": "正常",
      "gmtCreate": "2026-03-13 11:00:00",
      "gmtModify": "2026-03-13 11:00:00"
    }
  ]
}
```

## 返回字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | 报名记录ID |
| userId | Long | 用户ID |
| userName | String | 用户名（关联用户表） |
| loginName | String | 登录名（关联用户表） |
| applyId | Long | 活动ID |
| isPay | Integer | 支付状态：0-未支付，1-已支付 |
| isPayDesc | String | 支付状态描述 |
| userStatus | Integer | 用户状态：1-正常，0-禁用 |
| userStatusDesc | String | 用户状态描述 |
| gmtCreate | LocalDateTime | 创建时间 |
| gmtModify | LocalDateTime | 修改时间 |

## 查询场景

### 1. 查询所有报名学生（分页）

```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10
}
```

### 2. 仅查询已支付的学生

```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10,
  "isPay": 1
}
```

### 3. 仅查询未支付的学生

```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10,
  "isPay": 0
}
```

### 4. 按用户名模糊查询

```json
{
  "applyId": 1,
  "pageNum": 1,
  "pageSize": 10,
  "userName": "张"
}
```

## 实现逻辑

1. **查询报名记录**：从 `activity_apply_user` 表根据 `applyId` 查询报名记录
2. **批量查询用户信息**：从 `basic_user` 表批量查询用户信息
3. **数据组装**：将报名记录和用户信息组装成 VO
4. **分页返回**：支持分页和不分页两种方式

## 性能优化

- 使用批量查询避免 N+1 问题
- 一次性查询所有用户信息，然后通过 Map 匹配
- 支持分页查询，避免一次性加载大量数据

## 使用示例

### curl 示例

```bash
# 分页查询
curl -X POST http://localhost:9013/api/apply/users/page \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "applyId": 1,
    "pageNum": 1,
    "pageSize": 10
  }'

# 查询所有（不分页）
curl -X GET http://localhost:9013/api/apply/users/1 \
  -H "token: eyJhbGciOiJIUzI1NiJ9..."
```

### Postman 配置

**分页查询：**
- Method: `POST`
- URL: `http://localhost:9013/api/apply/users/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "applyId": 1,
    "pageNum": 1,
    "pageSize": 10,
    "isPay": 1
  }
  ```

**查询所有：**
- Method: `GET`
- URL: `http://localhost:9013/api/apply/users/1`
- Headers:
  ```
  token: {{token}}
  ```

## 错误处理

### 1. 活动ID为空

**请求：**
```json
{
  "pageNum": 1,
  "pageSize": 10
}
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "活动ID不能为空"
}
```

### 2. Token 缺失或无效

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

### 3. 权限不足

**响应：**
```json
{
  "longResult": -1,
  "objResult": "权限不符"
}
```

## 数据统计

通过本接口可以方便地统计：
- 活动报名总人数（查询所有记录）
- 已支付人数（isPay = 1）
- 未支付人数（isPay = 0）
- 用户状态分布（userStatus）

## 扩展功能

可以根据业务需求扩展：
- 添加退款状态筛选
- 添加报名时间段筛选
- 添加用户联系方式等字段
- 支持导出 Excel 功能
