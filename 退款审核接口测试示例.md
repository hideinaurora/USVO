# 退款审核接口测试示例

## 功能说明

管理员审核退款申请，支持审核通过或拒绝，并记录审核理由。

## 审核流程

```
1. 用户提交退款申请 → examineType = 0 (待审核)
2. 管理员审核退款 → examineType = 1 (通过) 或 2 (拒绝)
3. 创建审核记录 → 记录管理员ID、审核结果、审核理由
```

## 接口信息

**接口地址：** `POST /api/refund/examine`

**请求头：**
```
token: eyJhbGciOiJIUzI1NiJ9...
```

**请求参数：**
```json
{
  "refundId": 1,
  "examineType": 1,
  "reason": "审核理由说明"
}
```

## 测试场景

### 场景1：审核通过

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "用户提供了相关证明材料，同意退款申请"
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": true
}
```

**数据库变化：**
1. `activity_refund` 表的 `examine_type` 更新为 1
2. 新增一条 `activity_refund_examine` 审核记录

### 场景2：审核拒绝

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 2,
    "examineType": 2,
    "reason": "退款原因不充分，拒绝申请"
  }'
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": true
}
```

### 场景3：参数错误 - 退款申请ID为空

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "examineType": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "退款申请ID不能为空"
}
```

### 场景4：参数错误 - 审核类型为空

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "审核类型不能为空"
}
```

### 场景5：参数错误 - 审核类型无效

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 1,
    "examineType": 3,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "审核类型只能为1（审核通过）或2（审核拒绝）"
}
```

### 场景6：业务错误 - 退款申请不存在

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 99999,
    "examineType": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "退款申请不存在"
}
```

### 场景7：业务错误 - 退款申请已被审核

**请求：**
```bash
# 第一次审核
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 3,
    "examineType": 1,
    "reason": "第一次审核"
  }'

# 第二次审核（相同refundId）
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: eyJhbGciOiJIUzI1NiJ9..." \
  -d '{
    "refundId": 3,
    "examineType": 2,
    "reason": "第二次审核"
  }'
```

第二次审核响应：
```json
{
  "longResult": -1,
  "objResult": "该退款申请已被审核通过，无法再次审核"
}
```

### 场景8：认证错误 - Token缺失

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

### 场景9：认证错误 - Token无效

**请求：**
```bash
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: invalid_token_123" \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "测试"
  }'
```

**响应：**
```json
{
  "longResult": -1,
  "objResult": "登录失效请重新登录"
}
```

## Postman 测试步骤

### 1. 导入环境变量

创建环境变量 `Localhost`：
- `baseUrl`: `http://localhost:9013`
- `token`: (登录后自动填充)

### 2. 创建请求集合

#### 请求1：查询待审核的退款申请
- Method: `POST`
- URL: `{{baseUrl}}/api/refund/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "examineType": 0,
    "pageNum": 1,
    "pageSize": 10
  }
  ```
- 说明：获取所有待审核的退款申请

#### 请求2：审核退款通过
- Method: `POST`
- URL: `{{baseUrl}}/api/refund/examine`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "refundId": 1,
    "examineType": 1,
    "reason": "用户提供了相关证明材料，同意退款申请"
  }
  ```
- Tests:
  ```javascript
  var jsonData = pm.response.json();
  pm.test("审核成功", function() {
    pm.expect(jsonData.code).to.eql(200);
  });
  ```

#### 请求3：查询审核结果
- Method: `POST`
- URL: `{{baseUrl}}/api/refund/page`
- Headers:
  ```
  Content-Type: application/json
  token: {{token}}
  ```
- Body (raw JSON):
  ```json
  {
    "refundId": 1,
    "pageNum": 1,
    "pageSize": 10
  }
  ```
- 说明：验证审核状态是否更新

## 完整测试流程

### 步骤1：管理员登录
```bash
# 获取验证码
curl -X GET http://localhost:9013/api/admin/captcha

# 登录
curl -X POST http://localhost:9013/api/admin/login \
  -H "Content-Type: application/json" \
  -d '{
    "loginName": "admin",
    "loginPassword": "123456",
    "captchaKey": "从验证码接口获取",
    "captchaCode": "验证码"
  }'

# 保存返回的 accessToken
```

### 步骤2：查询待审核的退款申请
```bash
curl -X POST http://localhost:9013/api/refund/page \
  -H "Content-Type: application/json" \
  -H "token: 从步骤1获取的token" \
  -d '{
    "examineType": 0,
    "pageNum": 1,
    "pageSize": 10
  }'
```

### 步骤3：审核退款申请
```bash
# 审核通过
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: 从步骤1获取的token" \
  -d '{
    "refundId": 1,
    "examineType": 1,
    "reason": "同意退款申请"
  }'

# 或审核拒绝
curl -X POST http://localhost:9013/api/refund/examine \
  -H "Content-Type: application/json" \
  -H "token: 从步骤1获取的token" \
  -d '{
    "refundId": 2,
    "examineType": 2,
    "reason": "拒绝退款申请"
  }'
```

### 步骤4：查询审核结果
```bash
curl -X POST http://localhost:9013/api/refund/page \
  -H "Content-Type: application/json" \
  -H "token: 从步骤1获取的token" \
  -d '{
    "refundId": 1,
    "pageNum": 1,
    "pageSize": 10
  }'
```

验证返回的 `examineType` 和 `examineTypeDesc` 是否更新。

## 数据库验证

### 1. 查询退款申请状态
```sql
SELECT id, user_id, apply_id, examine_type, gmt_create, gmt_modify
FROM activity_refund
WHERE id = 1;
```

### 2. 查询审核记录
```sql
SELECT id, refund_id, examine_type, reason, admin_id, gmt_create
FROM activity_refund_examine
WHERE refund_id = 1
ORDER BY gmt_create DESC;
```

### 3. 查询完整退款信息（含用户和活动）
```sql
SELECT
    r.id AS refund_id,
    r.user_id,
    u.user_name,
    r.apply_id,
    a.apply_title,
    r.examine_type,
    r.refund_amount,
    e.reason AS examine_reason,
    e.admin_id
FROM activity_refund r
LEFT JOIN basic_user u ON r.user_id = u.id
LEFT JOIN activity_apply a ON r.apply_id = a.apply_id
LEFT JOIN activity_refund_examine e ON r.id = e.refund_id
WHERE r.id = 1;
```

## 注意事项

1. **审核限制**：只有待审核（examineType = 0）的退款申请才能审核
2. **一次性审核**：已审核的申请无法再次审核
3. **审核记录**：每次审核都会创建一条审核记录，记录审核管理员和审核理由
4. **事务处理**：审核操作使用事务，确保数据一致性
5. **权限要求**：只有管理员（ADMIN）才能进行审核操作

## 审核类型说明

| examineType | 说明 | 后续操作 |
|-------------|------|---------|
| 0 | 待审核 | 等待管理员审核 |
| 1 | 审核通过 | 退款成功，需要执行退款流程 |
| 2 | 审核拒绝 | 退款失败，通知用户审核结果 |

## 审核理由建议

### 审核通过的理由示例：
- "用户提供了相关证明材料，同意退款申请"
- "因特殊原因无法参加，同意退款"
- "活动取消，按规则退款"

### 审核拒绝的理由示例：
- "退款期限已过，不符合退款条件"
- "无法提供有效证明材料"
- "退款原因不充分，拒绝申请"
